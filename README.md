# imagnoter
A genuinely small, native annotation tool for drawing YOLO bounding boxes — draw and edit boxes, manage classes, save YOLO-format labels — without dragging an OpenCV, a second GUI toolkit, a browser, or a Python runtime along for the ride. One C file, GTK4, and nothing else.
